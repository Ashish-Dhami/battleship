import { renderBoard, renderPlayerLabel, renderStats, renderModal, updateGamePrefsUI } from 'UI/battlefieldUI'
import renderPlaceships from 'UI/placeshipsUI'

export { renderBoard, renderPlayerLabel, renderStats, renderModal, updateGamePrefsUI, renderPlaceships }
