import { renderBoard, renderPlayerLabel, renderStats, renderModal } from 'UI/battlefieldUI'
import renderPlaceships from 'UI/placeshipsUI'

export { renderBoard, renderPlayerLabel, renderStats, renderModal, renderPlaceships }
